class admin extends user {

    admin() //constructor
    {

    }

    async pullDriverListing()
    {
        //generate a local list from the database.
    }

    async pullDriverDetails(driver)
    {
        //pulls details of a specific driver from the local list.
    }

    async pullTripListing()
    {
        //generate a local list from the database.
    }

}